# Terraform devcontainer
Open folder in vscode. Open in devcontainer

Commands to run
```
terraform init
terraform plan
terraform apply
chmod 400 tfkey
ssh ec2-user@host -i tfkey
#terraform destroy
```